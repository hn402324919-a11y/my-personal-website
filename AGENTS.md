# AGENTS.md

本文件适用于仓库根目录及其全部子目录。开始修改前，先阅读 `PROJECT.md`，了解网站用途、当前结构、已知问题和开发优先级。

## 1. 项目开发规则

### 工作范围

- 这是陈旖旎的公开个人作品集。默认目标是保持现有内容、视觉语言和交互行为，并完成用户明确要求的改动。
- 不要擅自改写个人履历、联系方式、项目成果、品牌名称或作品图片；内容不确定时应保留原值并说明。
- 当前没有实际数据库、登录或表单业务。除非任务明确要求，不要启用 D1、R2、ChatGPT Auth 或新增后端状态。
- 不要顺手修改与当前任务无关的文件，也不要覆盖工作区中已有的未提交改动。

### 技术约定

- 使用 TypeScript、React 19、Next.js App Router 和 vinext 现有架构。
- 保持 TypeScript `strict`，不要用 `any`、`@ts-ignore` 或关闭 lint 规则来绕过问题。
- 优先使用函数组件、明确的 Props 类型和可清理的 Effect。
- 只在需要状态、Effect、DOM、Canvas、WebGL 或浏览器 API 的组件中使用 `"use client"`。
- 静态内容优先服务端渲染；不要无理由扩大客户端边界。
- 延续现有 `--ink`、`--text`、`--muted`、`--green`、`--shell` 等设计变量和深色荧光绿视觉体系。
- 新样式应放到对应组件样式或清晰的功能分区中。不要继续在 `globals.css` 末尾追加新的“最终覆盖”补丁。
- 当前同时存在两个锁文件。在项目明确统一包管理器之前，不要在一次依赖修改中分别使用 npm 和 pnpm；依赖变更必须说明更新了哪个锁文件。

### 常用命令

```bash
npm run dev
npm run lint
npm run build
npm test
```

也可以使用对应的 `pnpm dev`、`pnpm lint`、`pnpm build`、`pnpm test`，但一次任务中应保持同一种包管理器。

当前基线并非全绿：lint、test 的已知失败和 build 警告记录在 `PROJECT.md`。不要隐藏或跳过这些结果；如果本次任务涉及相关代码，应优先修复。任何改动都不得增加新的错误或警告。

## 2. 修改代码注意事项

### 页面与数据

- `app/page.tsx` 当前同时包含项目、经历、优势、工具数据和主要页面结构。编辑数据时同步检查类型、渲染位置、ARIA 标签和项目详情导航。
- 项目数量、案例数量或顺序变化时，检查：
  - 项目卡片编号和 `key`；
  - 案例 tab 的 `activeIndex`；
  - ArrowLeft、ArrowRight、Home、End 键盘逻辑；
  - 单图及多图 `src` 的展示；
  - `aria-labelledby`、`role="tablist"`、`role="tab"` 和 `role="tabpanel"` 的对应关系。
- 新增静态资源前确认文件真实存在于 `public/`，路径大小写完全一致；删除资源前先搜索 TSX、CSS、元数据和测试中的所有引用。
- 不要把私密信息、密钥、令牌或未授权素材提交到仓库。

### CSS 与响应式

- `app/globals.css` 有多层重复选择器，CSS 后部规则可能覆盖前部规则。修改前先搜索同一选择器的全部定义，并确认最终生效的声明。
- 触碰重复样式时，优先合并和删除失效规则；不要只在文件末尾再覆盖一次。
- 保持桌面、平板、手机三类布局可用，至少检查 `>900px`、`768–900px`、`<768px` 的关键状态。
- 不要破坏内容长图的宽高比、项目卡片构图、首屏文字层级和联系区对比度。
- 避免无意义的全局选择器。新增组件应使用稳定、语义化、局部化的 class。

### 动效与 WebGL

- 所有新增动画都必须尊重 `prefers-reduced-motion: reduce`。
- 悬停和指针特效必须先判断 `(hover: hover) and (pointer: fine)`，不能依赖触屏设备拥有 hover。
- Effect 中创建的事件监听器、`requestAnimationFrame`、Observer、GSAP context、Canvas 和 WebGL context 必须在 cleanup 中释放。
- 动画不得阻塞内容阅读、键盘操作或项目弹窗关闭。
- 避免为同一区域重复创建 WebGL/RAF 循环；新增动画依赖前先确认现有 GSAP、Motion 或 OGL 能否实现。

### 图片与性能

- 内容图片必须有准确的 `alt`；纯装饰图使用空 `alt` 并按需 `aria-hidden`。
- 不要直接加入未经压缩的超大图片。作品长图需要保真时，也应评估 WebP/AVIF、合理尺寸和懒加载。
- 首屏关键图与项目详情长图的加载策略应区分：首屏关注 LCP，详情图默认延迟加载。
- 引入 `next/image` 时要同时验证 vinext/Cloudflare 图片优化路径，不要只完成语法替换。
- 新增大型库或首屏依赖前说明理由，并检查客户端分包大小。

### 可访问性

- 可点击行为使用 `<button>` 或 `<a>`，不要只给 `<div>` 绑定点击事件。
- 保留跳转主内容链接、可见焦点、语义标题层级和中文 `lang="zh-CN"`。
- 修改项目弹窗时必须验证 Escape 关闭、背景滚动锁定、焦点进入/留在弹窗内、关闭后焦点恢复。
- 不能只依赖颜色或动画表达选中、成功或错误状态。
- Clipboard、IntersectionObserver、ResizeObserver、WebGL 等 API 需要考虑不可用或失败时的降级。

## 3. 避免破坏已有功能的要求

### 修改前

1. 运行 `git status --short`，识别并保留用户已有改动；
2. 搜索目标组件、选择器和资源的所有引用；
3. 记录本次任务相关的既有 lint、build、test 状态；
4. 对视觉改动先明确受影响的桌面、移动端和弹窗状态。

### 修改后最低验证

1. 运行 `npm run lint`；
2. 运行 `npm run build`；
3. 运行 `npm test`；
4. 如果既有基线仍有失败，交付说明必须区分“已有问题”和“本次新增问题”，不得声称全部通过；
5. 涉及 UI 时进行浏览器检查，至少覆盖：
   - 首屏加载和开场动画；
   - 固定导航与各锚点跳转；
   - 三张项目卡片打开详情；
   - 案例鼠标/触屏切换及键盘切换；
   - Escape 和关闭按钮；
   - 项目长图加载；
   - 电话链接和微信复制反馈；
   - 桌面与手机布局；
   - `prefers-reduced-motion` 模式；
6. 涉及元数据或部署配置时，检查 SSR HTML、Open Graph 图片 URL 和生产构建。

### 禁止事项

- 不删除或替换作品图片，除非用户明确要求且所有引用已经更新；
- 不用禁用 lint、删除失败测试或降低断言强度来制造“通过”；
- 不以重写整个页面作为局部需求的默认解决方案；
- 不在不了解后续覆盖规则时大范围格式化 `globals.css`；
- 不改变 `worker/index.ts` 的图片优化、Cloudflare 绑定或 `.openai/hosting.json` 项目标识，除非任务明确涉及部署；
- 不提交 `dist/`、`.next/`、`.wrangler/`、本地环境文件、日志或 `.DS_Store`；
- 不使用破坏性 Git 命令清理用户改动。

完成工作的标准是：用户要求已实现，现有作品内容与关键交互保持正常，没有新增 lint/build/test 回归，并清楚报告所有尚存的已知问题。
